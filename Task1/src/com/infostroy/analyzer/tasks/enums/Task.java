package com.infostroy.analyzer.tasks.enums;

public enum Task {

	FREQUENCY("frecuency"), LENGTH("length"), DUPLICATES("duplicates");

	private String task;

	Task(String t) {
		task = t;
	}
}
